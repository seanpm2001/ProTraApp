// Start of script
public class macOS_10_12(10.12) {
    // I am assuming OS X 10.8 is the first version of MacOS to support Swift.
    // Connecting 2 MacOS 10.12. programs together. Note that I have very little experience with Swift, so this script likely doesn't work correctly, and is currently just a demo script
    //Definitions
    NSString *str = @"",";
    str = [str stringByAppendingString:@""];
    appName1 = str("Enter app name here (the current one."); // Modify this line for your program
    appName2 = str("Enter second app name here (the one you are linking to."); // Modify this line as well for your program
    int ynInput; // The input method
    app2URL = string("/I don't know the Mac OS X File path/") // Incomplete. This method is for linking the 2 apps together, and jumping to the other program. Will prompt an install notice if it fails to reach. If the program is installed but reaching it results in an error, that means that ProTraApp was configured incorrectly
    // Main
    func switchPrograms() {
      print ("Switch programs/apps?\nThis application wants to jump from " + appName1() + " to " + appName2() + " Do you wish to proceed? (Y/N)");
    // ynInput = getChar( );
    // scanf("%s %d", str, &ynInput);
    ynInput = input_method_missing
    if (ynInput = "y" or "Y"):
      return app2URL();
    else:
      exit; // Exits the prompt
    return switchPrograms()
}} // End of main method
/* File info
* File version: `1 (Monday, March 8th 2021 at 8:28 pm)
* File type: Swift Source file (*.swift)
* Line count (including blank lines and compiler line): 30
* This script is incomplete, and needs lots of work */
// End of script
